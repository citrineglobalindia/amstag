{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm install --ignore-scripts && npm run build",
  "outputDirectory": "dist/client",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/|favicon.ico|robots.txt).*)", "destination": "/api/index" }
  ]
}
